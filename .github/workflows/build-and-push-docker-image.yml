name: Deploy to Kubernetes

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: nikita1996rao/proloaf10:latest

      - name: Deploy to Kubernetes
        env:
          KUBECONFIG: ${{ github.workspace }}/kind-kubeconfig.yaml
        run: |
          kubectl --kubeconfig=$KUBECONFIG --server=172.18.0.2:6443 --insecure-skip-tls-verify apply -f deployment.yaml
          kubectl --kubeconfig=$KUBECONFIG --server=172.18.0.2:6443 apply -f service.yaml
